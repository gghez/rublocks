{
  "path": "/",
  "method": "GET",
  "kind": "page",
  "template": "home.html",
  "layout": "main",

  "input": {
    "query": {
      "limit": { "type": "int", "default": 20, "max": 100 }
    }
  },

  "process": [
    {
      "name":     "posts",
      "block":    "db.find_many",
      "table":    "posts",
      "where":    { "published_at": { "is_not_null": true } },
      "order_by": "-published_at",
      "limit":    "$input.query.limit"
    }
  ],

  "view": {
    "page_title": "Recent posts",
    "tagline":    "Stories from the rublocks playground.",
    "posts":      "$posts"
  }
}
